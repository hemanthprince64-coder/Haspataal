---
trigger: always_on
description: Always read MEMORY.md at the start of every session/conversation.
---

## session-protocol

Always follow the session protocol:
- **Read First**: At the start of every session or conversation, you MUST read the `MEMORY.md` file before doing any other work. No exceptions.
- **Update Lessons**: After fixing any bug or completing a task, immediately add an entry to the Knowledge Base in `MEMORY.md`.
- **Commit Workflow**: When requested to "commit", follow the exact command workflow specified in `MEMORY.md`.
